{
  "name": "sales-god-mode",
  "version": "1.0.0",
  "description": "Offline inventory and sales management app",
  "private": true,
  "scripts": {
    "android": "npx cap sync android && npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}